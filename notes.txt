What is needed for a DOM example:

- [x] `let` expression
- [x] `do` expression
- [ ] array literals (for props)

For a more complete one:
- [ ] records

Do not obsess over efficiency of the generated code. In particular, using
`bind` is fine for now.
